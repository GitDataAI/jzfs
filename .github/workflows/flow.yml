name: Build and Deploy

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - '**'

jobs:
    build:
        uses: ./.github/workflows/build.yml
        secrets: inherit

    deployment:
        needs:
            - build
        uses: ./.github/workflows/deployment.yml
        secrets: inherit
